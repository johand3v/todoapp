import { ReapleSpacePipe } from './reaple-space.pipe';

describe('ReapleSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ReapleSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
