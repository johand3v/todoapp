import { ImagBrokenDirective } from './imag-broken.directive';

describe('ImagBrokenDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagBrokenDirective();
    expect(directive).toBeTruthy();
  });
});
